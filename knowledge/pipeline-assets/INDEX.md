# pipeline-assets/ — Índice

Fase que **UNIFICA** todo el bloque de arte 3D (modelado/blender/texturizado/rigging/animación) con **Unity 6**: el flujo COMPLETO de un asset de idea a prefab, el puente técnico entre extremos, la organización/versionado en disco y la ingeniería inversa de assets ajenos. No re-explica cada eslabón — lo ORDENA, lo conecta y lo cierra; el detalle de cada etapa vive en las bases hermanas y se enruta con `[ver: base/slug]`. **Empieza SIEMPRE por `pipeline-completo-3d`**: es el MAPA MAESTRO (el conductor) que ordena las etapas 0–11, dice qué base cargar en cada una, define el gate acumulativo y ramifica estático vs animado; salta a la base de la etapa cuando toque ejecutarla. Los otros tres son especializaciones del puente: `blender-unity-bridge` (el contrato técnico del cruce), `organizacion-versionado-3d` (filesystem + git/LFS) y `aprender-de-assets` (ingeniería inversa → ficha-spec). Cargar SOLO lo que la tarea pida. Stack de referencia: Blender 5.2 LTS + Unity 6 (6000.x) / URP; datos verificados contra docs.unity3d.com y binario Blender 5.2 headless (jul-2026).

| Archivo | Qué contiene | Cuándo cargarlo |
|---|---|---|
| **pipeline-completo-3d.md** | **El MAPA MAESTRO idea→prefab (el índice ejecutable, no ejecuta el detalle).** Las etapas 0–11 (0 .blend a escala → 1 referencia → 2 blockout → 3 modelado 1º/2º/3º → 4 cleanup → 5 UV → 6a bake / 6b texturizado PBR → **+RIG +ANIM si deforma** → 7 export FBX → 8 import → 9 material URP → 10 Prefab Variant → 11 verificación en engine) con base ejecutora, estado de salida y gate por fila (§1); la ramificación **estático vs animado** y dónde se decide (ANTES de la Etapa 3, la topología de deformación no se parchea) (§2); el puente Blender→Unity consolidado —escala/ejes/normales/normal-map/rig/clips/GUID— (§3); el asset como **Prefab Variant** del model prefab y qué componentes ensambla por tipo, LODGroup por naming `_LOD0..n` (§4.1), colliders primitivo/`UCX_` nunca render mesh (§4.2); el **gate acumulativo** estado-de-salida + checkpoint por código por etapa (§5); el presupuesto de **4 ejes** (tris+texturas+huesos+clips) + snippet `bpy` para medir el FBX real (§6); método de aprender de assets (§7); el loop de iteración .blend→reimport sin rehacer el prefab + AssetPostprocessor (§8); el **checklist game-ready UNIFICADO** de malla/UV/textura/rig/clips/material/prefab/engine (§9) | **PRIMERO y siempre**, al ARRANCAR o AUDITAR cualquier asset 3D de punta a punta: da la secuencia completa, ordena las etapas, decide a qué base saltar y define el gate. Es el conductor; cada etapa la ejecuta su base hermana. Carga esto y salta cuando toque ejecutar |
| **blender-unity-bridge.md** | **El CONTRATO técnico del cruce Blender↔Unity.** Los 5 tramos y su dueño; la **tabla DEFINITIVA del handoff** (unidad metros / escala 100× vía `FBX_SCALE_UNITS` → File Scale 1 / ejes Z-up→Y-up / transforms aplicados / pivot por uso) con criterio de aceptación + **el checklist que mata el -89.98** por tipo (props `bakeAxisConversion`; rigged nunca `bake_space_transform`); FBX vs glTF (FBX para el prefab, glTF solo runtime); qué cruza el FBX y qué se **reconstruye** en Unity; materiales en el cruce (solo nombre+slot; el material real es de Unity: Extract / `SearchAndRemapMaterials` / `OnAssignMaterialModel`); colliders `UCX_` (convención Unreal, Unity NO la procesa → postprocessor); sockets (Empties); LODs→LODGroup auto; el **AssetPostprocessor completo** (4 hooks de modelo, código C# verificado); el **roundtrip robusto** (GUID en el `.meta` anclado al path, sobrescribir mismo path, congelar nombres); estructura source/.blend vs exported/FBX + git LFS; validación end-to-end en Unity | Cuando un asset CRUZA de Blender a Unity y hay que cerrar el flujo: fijar escala/ejes/unidades definitivos, resolver materiales/colliders/LODs/sockets en el cruce, hacer el re-export sin romper prefabs, o automatizar los import settings con un `AssetPostprocessor`. El nodo TÉCNICO del puente; el detalle de cada extremo vive en `blender/import-export`, `pipeline/arte-a-unity`, `unity/assets-pipeline-git` |
| **organizacion-versionado-3d.md** | **La capa FILESYSTEM + VCS del puente.** Los dos árboles que NO se versionan igual (**SOURCE** `.blend`/`.psd`/high-poly FUERA de `Assets/` = fuente de verdad; **EXPORTED** FBX/tex/mat/prefab DENTRO = derivado reproducible); topología de carpetas `_Game/Art/{Models,Textures,Materials,Prefabs}` + `sources/`; `.blend` dentro vs fuera de `Assets/` (proprietary format exige Blender en cada máquina/CI) + truco carpeta `Source~/`; **naming cross-tool** que sobrevive el pipeline (un namespace `crate_wood` en .blend/objeto/malla/material/FBX/textura/.mat/prefab; malla nombrada como su objeto; `.001` sucio rompe el remap de materiales); qué cruza el FBX y sus trampas de escala/ejes/LOD; **git para binarios** (LFS + `.gitattributes` unificado, `.meta` en el MISMO commit, `unityyamlmerge` para YAML); trazabilidad prefab→`.blend` (naming espejo + `AssetImporter.userData` + manifest); biblioteca reusable entre proyectos por **paquete UPM / Asset Browser** (nunca copiar archivos); versionado de un asset en su vida (`_vNNN` solo en release); colaboración sin pisar binarios (`git lfs lock`) | Al montar la estructura de carpetas de un proyecto de arte 3D, decidir dónde viven los source vs exported, fijar el naming que sobrevive el cruce, configurar git+LFS, trazar un prefab de vuelta a su `.blend`, armar una biblioteca reutilizable entre proyectos, o coordinar quién toca qué binario en equipo/multi-instancia |
| **aprender-de-assets.md** | **La INGENIERÍA INVERSA de un asset ya hecho → ficha-spec de convenciones** (no produce el asset, produce la spec). Las 3 superficies de inspección (`.meta`=intención de import, malla/FBX=realidad geométrica, `.prefab`=ensamble) y la regla **texto primero, editor después**; leer el `.meta` por grep (`sRGBTexture`, `textureType` 0/1/8, `animationType` **2=Generic/3=Humanoid**, escala, `clipAnimations`); leer `.prefab`/`.unity` YAML (`--- !u!<classID> &<fileID>`, LODGroup `!u!205`, materiales por `guid`); **medir la malla real** por `bpy` (tris/verts/dims/pivote/UV/huesos) o por editor-script en Unity (`Mesh.vertexCount` = el conteo real del engine con splits, manda sobre el del DCC); **texel density px/m**; leer material/shader (contrato de canales, MetallicSmoothness en alpha, OpenGL vs DirectX por `flipGreenChannel`); leer rig+clips (huesos/Avatar/`sourceAvatar`; el **loop está en `clipAnimations[i].loopTime` del importer**, no en el clip); leer atlas/spritesheet 2D (PPU, 9-slice); el runbook por coste (texto→Blender→Unity); la **ficha de convenciones** (plantilla §10, cada celda con fuente o "no medido"); ética/licencia (medir el estándar sí, copiar/redistribuir los bits no) | Cuando tienes un asset TERMINADO (FBX, prefab, mesh, material, atlas 2D, AnimatorController) y necesitas extraer sus **convenciones técnicas** para producir arte consistente con él — igualar el estándar de un pack, de un asset de referencia del cliente, o de lo que ya está en el proyecto. Fase de ingeniería inversa; su output (la ficha) lo consume `pipeline-completo-3d` como spec de producción |

*Cross-refs `[ver: ...]` de esta base verificados contra archivos reales (36 destinos distintos): **0 colgantes** — todos resuelven. Refs internos a §secciones dentro de cada archivo son auto-referencias válidas. Convención: `[ver: slug]` cruza esta carpeta; `[ver: base/slug]` va a las hermanas (blender/, unity/, pipeline/, modelado/, pipeline-3d/, texturizado/, rigging/, animacion3d/, animacion-blender/) y estos archivos las ASUMEN, no las repiten.*
